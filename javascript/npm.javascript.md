# npm

```sh
# Search for available packages containing the specified keyword
npm search react

# List the available versions of a given package
npm view react versions

# Checks the installed version of a current package
npm list -g @angular/cli

# Install/upgrade a specific version 
npm install -g @angular/cli@11.2.0
```
